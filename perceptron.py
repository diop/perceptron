import numpy as np 

class Perceptron(object):
    def __init__(self, n_inputs=3, weights=1, bias=0):
        self.n_inputs = n_inputs
        self.weights = weights 
        self.bias = bias

    def weighted_sum(features):
        inputs = []

        return inputs

    def activation(sum):
        pass

    def predict(X):
        pass

    def compute_error(X, y):
        pass

    def update_weights(error, X):
        pass 

    def fit(X, y):
        pass


nu_perceptron = Perceptron()
print(nu_perceptron.n_inputs, nu_perceptron.weights, nu_perceptron.bias)
